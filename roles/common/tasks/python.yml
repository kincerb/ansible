---
- name: Install python packages as user
  become: no
  pip: 
    name: '{{ package }}'
    extra_args: --user
  vars:
    package:
      - ansible
      - black
      - powerline-status
      - psutil
      - pyuv
      - ranger-fm
...
