---
- name: Install python packages as user
  become: false
  pip: 
    executable: pip3
    extra_args: --user
    name:
      - pipx
      - psutil

- name: Install python packages as user
  become: false
  pip: 
    executable: pipx
    name:
      - black
      - docformatter
      - flake8
      - isort
      - jedi-language-server
      - ranger-fm
...
