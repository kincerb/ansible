---
- name: Install needed packages
  apt:
    name: '{{ package }}'
  vars:
    package:
      - apt-transport-https
      - ca-certificates
      - curl
      - git
      - python3-pip
      - python3-venv
      - rsync
      - software-properties-common
      - tmux
      - unzip
      - vim
...
