---
- name: Install needed packages
  apt:
    name: '{{ package }}'
  vars:
    package:
      - apt-transport-https
      - ca-certificates
      - curl
      - python3-pip
      - python3-venv
      - software-properties-common
...
